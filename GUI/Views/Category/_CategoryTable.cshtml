@using GUI.Models.DTOs
@model IEnumerable<CategoryDTO>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Tên</th>
            <th>Trạng thái</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var category in Model)
        {
            <tr>
                <td>@category.Name</td>
                <td>@category.StatusText</td>
                <td>
                    <button class="btn btn-primary me-1" onclick="showDeatail('@category.Id')">Sửa</button>
                    <a class="btn btn-info" href="/Store?s=@category.Id">Chi tiết</a>
                </td>
            </tr>
        }
    </tbody>
</table>