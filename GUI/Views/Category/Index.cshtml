@using GUI.Models.DTOs
@{
    ViewData["Title"] = "Quản lý danh mục";
}

@model IEnumerable<CategoryDTO>

<div class="container mt-4">
    <h2 class="text-center text-primary fw-bold">Danh Sách Danh Mục</h2>
    <hr />
    <div class="row mb-3">
        <div class="col-6">
            <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#categoryModal" onclick="clear()">Thêm mới</button>
        </div>
        <div class="col-6 d-flex justify-content-end">
            <input class="form-control me-2" placeholder="Nhập tên danh mục ..." id="categorySearch" style="width: 300px;" />
            <button class="btn btn-primary ms-2" onclick="search()">Tìm kiếm</button>
            <button class="btn btn-danger ms-2" onclick="reset()">Reset</button>
        </div>
    </div>

    <div id="categoryTable">
        @await Html.PartialAsync("_CategoryTable", Model)
    </div>
</div>

<div class="modal fade" id="categoryModal" tabindex="-1" aria-labelledby="categoryModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content" id="modalContent">
            @await Html.PartialAsync("_CategoryModal", new CategoryDTO())
        </div>
    </div>
</div>

@section scripts {
    <script src="~/lib/toastr/toastr.min.js"></script>
    <script src="~/js/category.js"></script>
}