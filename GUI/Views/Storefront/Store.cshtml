@using System.Globalization
@model GUI.Models.DTOs.Product_DTO.Views.IndexObject
@{
    Layout = "_LayoutStore";
}

@if (Model.Data is not null && Model.Data.LstProduct is not null && Model.Data.LstProduct.Any())
{
    <!-- SECTION -->
    <div class="section">
        <!-- container -->
        <div class="container">
            <!-- row -->
            <div class="row">
                <!-- ASIDE -->
                <div id="aside" class="col-md-3" style="margin-top:75px;">
                    <!-- aside Widget -->
                    <div class="aside">
                        <h3 class="aside-title">Lọc sản phẩm</h3>

                        <!-- Tên sản phẩm filter -->
                        <div class="filter-item">
                            <h4 class="filter-title">Tên sản phẩm</h4>
                            <input id="filter-name" class="form-control" type="text" placeholder="Nhập tên sản phẩm">
                        </div>

                        <!-- Price filter -->
                        <div class="filter-item">
                            <h4 class="filter-title">Giá</h4>
                            <div class="price-filter">
                                <div id="price-slider"></div>
                                <div class="input-number price-min">
                                    <input id="price-min" type="number" name="min" data-value="@(Model?.Data?.LowestPrice ?? 0)" value="@ViewBag.PriceFrom" class="form-control filter-input">
                                    <span class="qty-up">+</span>
                                    <span class="qty-down">-</span>
                                </div>
                                <span>-</span>
                                <div class="input-number price-max">
                                    <input id="price-max" type="number" name="max" data-value="@(Model?.Data?.HighestPrice ?? 0)" value="@ViewBag.PriceTo" class="form-control filter-input">
                                    <span class="qty-up">+</span>
                                    <span class="qty-down">-</span>
                                </div>
                            </div>
                        </div>

                        <!-- Status filter -->
                        <div class="filter-item">
                            <h4 class="filter-title">Trạng thái</h4>
                            <select id="filter-status" class="form-control filter-input">
                                <option value="">Tất cả</option>
                                <option value="available">Còn hàng</option>
                                <option value="out-of-stock">Hết hàng</option>
                            </select>
                        </div>

                        @* <!-- Category filter -->
                        <div class="filter-item">
                            <h4 class="filter-title">Danh mục</h4>
                            <select id="filter-category" class="form-control filter-input">
                                <option value="">Tất cả</option>
                                @if (Model.Data.LstProduct != null)
                                {
                                    var categories = Model.Data.LstProduct.Select(p => p.CategoryName).Distinct();
                                    foreach (var category in categories)
                                    {
                                        <option value="@category">@category</option>
                                    }
                                }
                            </select>
                        </div> *@

                        @* <!-- Size filter -->
                        <div class="filter-item">
                            <h4 class="filter-title">Kích thước</h4>
                            <div class="size-filter">
                                <div class="input-checkbox">
                                    <input type="checkbox" id="size-s" class="filter-size" value="S">
                                    <label for="size-s">
                                        <span></span>S
                                    </label>
                                </div>
                                <div class="input-checkbox">
                                    <input type="checkbox" id="size-m" class="filter-size" value="M">
                                    <label for="size-m">
                                        <span></span>M
                                    </label>
                                </div>
                                <div class="input-checkbox">
                                    <input type="checkbox" id="size-l" class="filter-size" value="L">
                                    <label for="size-l">
                                        <span></span>L
                                    </label>
                                </div>
                                <div class="input-checkbox">
                                    <input type="checkbox" id="size-xl" class="filter-size" value="XL">
                                    <label for="size-xl">
                                        <span></span>XL
                                    </label>
                                </div>
                            </div>
                        </div> *@

                        @* <!-- Color filter -->
                        <div class="filter-item">
                            <h4 class="filter-title">Màu sắc</h4>
                            <div class="color-filter">
                                <div class="input-checkbox">
                                    <input type="checkbox" id="color-black" class="filter-color" value="Black">
                                    <label for="color-black" style="background-color: #000">
                                        <span></span>
                                    </label>
                                </div>
                                <div class="input-checkbox">
                                    <input type="checkbox" id="color-white" class="filter-color" value="White">
                                    <label for="color-white" style="background-color: #fff; border: 1px solid #e4e7ed">
                                        <span></span>
                                    </label>
                                </div>
                                <div class="input-checkbox">
                                    <input type="checkbox" id="color-red" class="filter-color" value="Red">
                                    <label for="color-red" style="background-color: #D10024">
                                        <span></span>
                                    </label>
                                </div>
                                <div class="input-checkbox">
                                    <input type="checkbox" id="color-blue" class="filter-color" value="Blue">
                                    <label for="color-blue" style="background-color: #1E90FF">
                                        <span></span>
                                    </label>
                                </div>
                                <div class="input-checkbox">
                                    <input type="checkbox" id="color-green" class="filter-color" value="Green">
                                    <label for="color-green" style="background-color: #2E8B57">
                                        <span></span>
                                    </label>
                                </div>
                            </div>
                        </div> *@

                        <!-- Display options -->
                        
                    </div>
                    <!-- /aside Widget -->
                </div>
                <!-- /ASIDE -->
                <div class="col-md-2">
                    <div class="form-group d-flex flex-column h-100">
                        <label for="page-display" class="filter-label mb-2">Hiển thị</label>
                        <select id="page-display" class="form-control">
                            @foreach (var item in ViewBag.TakeOptions)
                            {
                                if (item == ViewBag.Take)
                                {
                                    <option value="@item" selected>@item</option>
                                }
                                else
                                {
                                    <option value="@item">@item</option>
                                }
                            }
                        </select>
                    </div>
                </div>
                <!-- STORE -->
                <div id="store" class="col-md-9">
                    <!-- store products -->
                    
                    <div class="row" id="product-list">
                        @foreach (var item in Model.Data.LstProduct)
                        {
                            <!-- product -->
                            <div class="col-md-4 col-xs-6 product-item"
                                 data-name="@item.Name"
                                 data-category="@item.CategoryName"
                                 data-status="@(item.Quantity > 0 ? "available" : "out-of-stock")"
                                 data-price="@item.Price"
                                 data-sizes="S,M,L"
                                 data-colors="Black,White,Red">
                                <div class="product">
                                    <div class="product-img">
                                        <img src="@item.Image" alt="" style="cursor: pointer" onclick="redirectToDetail('@item.Id')">
                                        @if (item.Quantity <= 0)
                                        {
                                            <div class="product-label" style="width: 100%; height: 100%; position: absolute; top: 0; left: 0; display: flex; justify-content: center; align-items: center; background-color: rgba(255, 255, 255, 0.7);">
                                                <span style="padding: 8px 15px; background-color: rgba(0, 0, 0, 0.6); color: #fff; font-size: 14px; letter-spacing: 1px; font-weight: 500;">HẾT HÀNG</span>
                                            </div>
                                        }
                                    </div>
                                    <div class="product-body">
                                        <p class="product-category">@item.CategoryName</p>
                                        <h3 class="product-name"><a href="#" onclick="redirectToDetail('@item.Id')">@item.Name</a></h3>
                                        <h4 class="product-price">
                                            @(item.Price.ToString("C", CultureInfo.GetCultureInfo("vi-VN")))
                                        </h4>
                                        <div class="product-rating">
                                        </div>
                                        <div class="product-btns">
                                            <button class="quick-view" onclick="redirectToDetail('@item.Id')">
                                                <i class="fa fa-eye"></i>
                                                <span class="tooltipp">xem chi tiết</span>
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <!-- /product -->
                        }
                    </div>
                    <!-- /store products -->
                    <!-- store bottom filter -->
                    <div class="store-filter clearfix">
                        <ul class="store-pagination">
                            @if (ViewBag.CurrentPage != 0)
                            {
                                <li class="pagination-btn" data-page="@(ViewBag.CurrentPage - 1)"><a href="#"><i class="fa fa-angle-left"></i></a></li>
                            }
                            @for (int i = ViewBag.StartPage; i <= ViewBag.TopPage; i++)
                            {
                                if (i >= 0 && i <= ViewBag.TotalPages)
                                {
                                    <li data-page="@i" class="pagination-btn @(i == ViewBag.CurrentPage ? "active" : "")">@(i + 1)</li>
                                }
                            }
                            @if (ViewBag.TotalPages != ViewBag.CurrentPage)
                            {
                                <li class="pagination-btn" data-page="@(ViewBag.CurrentPage + 1)"><a href="#"><i class="fa fa-angle-right"></i></a></li>
                            }
                        </ul>
                    </div>
                    <!-- /store bottom filter -->
                </div>
                <!-- /STORE -->
            </div>
            <!-- /row -->
        </div>
        <!-- /container -->
    </div>
    <!-- /SECTION -->
    <!-- Thêm JavaScript và CSS -->
    <style>
        .product-img {
            position: relative;
            overflow: hidden;
        }

        .product:hover .product-label {
            opacity: 1;
        }

        .filter-item {
            margin-bottom: 20px;
        }

        .filter-title {
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 10px;
            color: #444;
        }

        .filter-input {
            border: 1px solid #ccc;
            padding: 8px 12px;
            font-size: 14px;
            width: 100%;
            height: 38px;
            border-radius: 4px;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

            .filter-input:focus {
                border-color: #D10024;
                box-shadow: 0 0 5px rgba(209, 0, 36, 0.3);
                outline: none;
            }

        .color-filter {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .size-filter {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .input-checkbox label {
            position: relative;
            display: inline-block;
            cursor: pointer;
            padding-left: 30px;
            margin-bottom: 0;
        }

        .color-filter .input-checkbox label {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            padding-left: 0;
            margin-right: 10px;
        }
    </style>

    <script>
        // Hàm lọc sản phẩm
        function filterProducts() {
            const nameFilter = document.getElementById('filter-name').value.toLowerCase();
            const statusFilter = document.getElementById('filter-status').value;
            const categoryFilter = document.getElementById('filter-category').value;
            const priceFrom = parseFloat(document.getElementById('price-min').value) || 0;
            const priceTo = parseFloat(document.getElementById('price-max').value) || Infinity;

            // Lấy sizes được chọn
            const selectedSizes = [];
            document.querySelectorAll('.filter-size:checked').forEach(checkbox => {
                selectedSizes.push(checkbox.value);
            });

            // Lấy colors được chọn
            const selectedColors = [];
            document.querySelectorAll('.filter-color:checked').forEach(checkbox => {
                selectedColors.push(checkbox.value);
            });

            const items = document.querySelectorAll('.product-item');
            items.forEach(item => {
                const name = item.getAttribute('data-name').toLowerCase();
                const status = item.getAttribute('data-status');
                const category = item.getAttribute('data-category');
                const price = parseFloat(item.getAttribute('data-price'));
                const itemSizes = item.getAttribute('data-sizes').split(',');
                const itemColors = item.getAttribute('data-colors').split(',');

                const matchesName = nameFilter ? name.includes(nameFilter) : true;
                const matchesStatus = statusFilter ? status === statusFilter : true;
                const matchesCategory = categoryFilter ? category === categoryFilter : true;
                const matchesPrice = price >= priceFrom && price <= priceTo;

                // Kiểm tra xem có size phù hợp không
                let matchesSize = true;
                if (selectedSizes.length > 0) {
                    matchesSize = selectedSizes.some(size => itemSizes.includes(size));
                }

                // Kiểm tra xem có màu phù hợp không
                let matchesColor = true;
                if (selectedColors.length > 0) {
                    matchesColor = selectedColors.some(color => itemColors.includes(color));
                }

                if (matchesName && matchesStatus && matchesCategory && matchesPrice && matchesSize && matchesColor) {
                    item.style.display = '';
                    // Thêm hiệu ứng fade in
                    item.style.opacity = '0';
                    setTimeout(() => {
                        item.style.transition = 'opacity 0.5s ease';
                        item.style.opacity = '1';
                    }, 50);
                } else {
                    item.style.display = 'none';
                    item.style.opacity = '0';
                }
            });
        }

        // Thêm sự kiện cho tất cả các filter input
        const filterInputs = document.querySelectorAll('.filter-input, .filter-size, .filter-color');
        filterInputs.forEach(input => {
            if (input.type === 'checkbox') {
                input.addEventListener('change', filterProducts);
            } else {
                input.addEventListener('input', filterProducts);
            }
        });

        // Xử lý chuyển trang chi tiết sản phẩm
        function redirectToDetail(productId) {
            window.location.href = `/productDetail?id=${productId}`;
        }

        // Khởi tạo filter ban đầu
        document.addEventListener('DOMContentLoaded', function() {
            // Đặt sự kiện cho nút +/- của price filters
            document.querySelectorAll('.qty-up').forEach(btn => {
                btn.addEventListener('click', function() {
                    const input = this.parentElement.querySelector('input');
                    input.value = parseInt(input.value) + 1;
                    filterProducts();
                });
            });

            document.querySelectorAll('.qty-down').forEach(btn => {
                btn.addEventListener('click', function() {
                    const input = this.parentElement.querySelector('input');
                    if (parseInt(input.value) > 0) {
                        input.value = parseInt(input.value) - 1;
                        filterProducts();
                    }
                });
            });
        });
    </script>
}
else
{
    <!-- HOT DEAL SECTION -->
    <div class="section">
        <!-- container -->
        <div class="container">
            <!-- row -->
            <div class="row">
                <div class="col-md-12">
                    <div class="hot-deal">
                        <ul class="hot-deal-countdown">
                            <li>
                                <div>
                                    <h3>
                                        <svg xmlns="http://www.w3.org/2000/svg" width="50" height="50" fill="currentColor" class="bi bi-emoji-frown" viewBox="0 0 16 16">
                                            <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14m0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16" />
                                            <path d="M4.285 12.433a.5.5 0 0 0 .683-.183A3.5 3.5 0 0 1 8 10.5c1.295 0 2.426.703 3.032 1.75a.5.5 0 0 0 .866-.5A4.5 4.5 0 0 0 8 9.5a4.5 4.5 0 0 0-3.898 2.25.5.5 0 0 0 .183.683M7 6.5C7 7.328 6.552 8 6 8s-1-.672-1-1.5S5.448 5 6 5s1 .672 1 1.5m4 0c0 .828-.448 1.5-1 1.5s-1-.672-1-1.5S9.448 5 10 5s1 .672 1 1.5" />
                                        </svg>
                                    </h3>
                                </div>
                            </li>
                        </ul>
                        <h2 class="text-uppercase">Xin lỗi!</h2>
                        <p>Không tìm thấy sản phẩm bạn muốn mua!</p>
                        <a class="primary-btn cta-btn" href="Store">Quay lại cửa hàng</a>
                    </div>
                </div>
            </div>
            <!-- /row -->
        </div>
        <!-- /container -->
    </div>
    <!-- /HOT DEAL SECTION -->
}